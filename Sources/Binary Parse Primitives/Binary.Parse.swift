// Binary.Parse.swift
// swift-binary-parser-primitives
//
// Namespace for bytes parsing capabilities.
//
// Relocated from swift-binary-primitives 2026-05-20 — the Parse namespace
// is parser-domain, not binary-domain. binary-primitives held the empty
// namespace declaration only as an extension anchor; parser-primitives
// owns every member, so the namespace itself belongs here.

extension Binary {
    public enum Parse {}
}
